name: Setup pnpm
description: Install pnpm through the canonical fleet setup.

runs:
  using: composite
  steps:
    - name: Hydrate fleet tools
      shell: bash
      run: node scripts/repo/bootstrap/fleet.mjs --preserve-tracked
    - name: Set up pnpm
      uses: ./.github/actions/fleet/setup
