{
  "private": true,
  "scripts": {
    "check": "node scripts/check.mts",
    "coverage": "node scripts/coverage.mts",
    "fix": "node scripts/lint.mts --fix",
    "gen": "node scripts/repo/gen/logo.mts",
    "gen:logo": "node scripts/repo/gen/logo.mts",
    "gen:svg": "node scripts/repo/gen/optimize-svg.mts",
    "lint": "node scripts/lint.mts",
    "prepare": "node scripts/install-git-hooks.mts",
    "release": "node scripts/release.mts",
    "test:assets": "node --test scripts/repo/gen/logo.test.mts"
  },
  "devDependencies": {
    "@resvg/resvg-wasm": "catalog:",
    "svgo": "catalog:",
    "typescript": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": ">=11.0.0 <12.0.0",
      "onFail": "error"
    }
  }
}
